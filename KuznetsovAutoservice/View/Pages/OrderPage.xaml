<Page x:Class="KuznetsovAutoservice.View.Pages.OrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:KuznetsovAutoservice.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="380" d:DesignWidth="590"
      Title="OrderPage" Background="AliceBlue">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="30" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="150" />
        </Grid.ColumnDefinitions>


        <ListView Name="OrderLv" Grid.Row="1" Grid.RowSpan="3" Margin="10 0 10 0" FontSize="16" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="80" DisplayMemberBinding="{Binding OrderID}"/>
                    <GridViewColumn Header="Услуга" Width="80" DisplayMemberBinding="{Binding Services.ServiceName}"/>
                    <GridViewColumn Header="Автомобиль" Width="100" DisplayMemberBinding="{Binding Cars.Brand}"/>
                    <GridViewColumn Header="Сотрудник" Width="100" DisplayMemberBinding="{Binding Employees.Position}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
