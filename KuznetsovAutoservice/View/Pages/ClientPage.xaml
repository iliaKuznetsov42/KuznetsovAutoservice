<Page x:Class="KuznetsovAutoservice.View.Pages.ClientPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:KuznetsovAutoservice.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="380" d:DesignWidth="590"
      Title="ClientPage" Background="AliceBlue">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="30" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="150" />
        </Grid.ColumnDefinitions>

        <ListView Name="ClientLv" Grid.Row="1" Grid.ColumnSpan="4" Grid.RowSpan="3" Margin="10 0 10 0" FontSize="16">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Имя" Width="80" DisplayMemberBinding="{Binding Clients.FirstName}"/>
                    <GridViewColumn Header="Фамилия" Width="80" DisplayMemberBinding="{Binding Clients.LastName}"/>
                    <GridViewColumn Header="Телефон" Width="100" DisplayMemberBinding="{Binding Clients.Phone}"/>
                    <GridViewColumn Header="Почта" Width="80" DisplayMemberBinding="{Binding Clients.Email}"/>
                    <GridViewColumn Header="Марка" Width="80" DisplayMemberBinding="{Binding Brand}"/>
                    <GridViewColumn Header="Модель" Width="80" DisplayMemberBinding="{Binding Model}"/>
                    <GridViewColumn Header="Гос Номер" Width="80" DisplayMemberBinding="{Binding LicensePlate}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
